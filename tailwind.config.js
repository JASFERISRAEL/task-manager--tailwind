module.exports = {
  content: [
    "./public/*.html",   // Scans HTML files in the 'public' folder
    "./public/*.js"      // Scans JS files in the 'public' folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
